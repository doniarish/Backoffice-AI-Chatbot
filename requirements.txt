# Core API framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# LangChain + LangGraph orchestration
langchain>=0.2.0
langchain-core>=0.2.0
langchain-openai>=0.1.0
langgraph>=0.1.0

# RAG / Vector search (local — no API key required for embeddings)
sentence-transformers>=3.0.0
faiss-cpu>=1.8.0

# Session management (Redis optional — falls back to in-memory)
redis>=5.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0    # for FastAPI TestClient

# Type annotations
typing-extensions>=4.9.0

